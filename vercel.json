{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "npm run validate:production-env && npm run update-app-version -- --date-only && npm run build",
  "outputDirectory": "apps/web/.next"
}
