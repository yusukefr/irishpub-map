# Database schema (generated)

> **Do not edit this file manually.** Run `npm run generate:database-schema` with `DATABASE_URL` set to regenerate it from the current Neon PostgreSQL schema.
> The generator performs read-only catalog queries and never writes the connection string or table data.

This document records the physical schema in PostgreSQL `public`: tables, columns, constraints (including foreign keys), and indexes.

## content_entries

### Columns

| Column         | Type                       | Nullable | Default             |
| -------------- | -------------------------- | -------- | ------------------- |
| `id`           | `uuid`                     | no       | `gen_random_uuid()` |
| `kind`         | `text`                     | yes      | —                   |
| `slug`         | `text`                     | yes      | —                   |
| `category`     | `text`                     | yes      | —                   |
| `status`       | `text`                     | no       | —                   |
| `published_at` | `timestamp with time zone` | yes      | —                   |
| `created_at`   | `timestamp with time zone` | no       | `now()`             |
| `updated_at`   | `timestamp with time zone` | no       | `now()`             |

### Constraints

| Name                                      | Type        | Definition                                                                                                           |
| ----------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| `content_entries_category_check`          | CHECK       | `CHECK (btrim(category) <> ''::text)`                                                                                |
| `content_entries_kind_check`              | CHECK       | `CHECK (btrim(kind) <> ''::text)`                                                                                    |
| `content_entries_kind_slug_key`           | UNIQUE      | `UNIQUE (kind, slug)`                                                                                                |
| `content_entries_pkey`                    | PRIMARY KEY | `PRIMARY KEY (id)`                                                                                                   |
| `content_entries_publication_state_check` | CHECK       | `CHECK (status = 'draft'::text AND published_at IS NULL OR status = 'published'::text AND published_at IS NOT NULL)` |
| `content_entries_slug_check`              | CHECK       | `CHECK (btrim(slug) <> ''::text)`                                                                                    |
| `content_entries_status_check`            | CHECK       | `CHECK (status = ANY (ARRAY['draft'::text, 'published'::text]))`                                                     |

### Indexes

| Name                            | Definition                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `content_entries_kind_slug_key` | `CREATE UNIQUE INDEX content_entries_kind_slug_key ON public.content_entries USING btree (kind, slug)` |
| `content_entries_pkey`          | `CREATE UNIQUE INDEX content_entries_pkey ON public.content_entries USING btree (id)`                  |

## content_translations

### Columns

| Column          | Type                       | Nullable | Default |
| --------------- | -------------------------- | -------- | ------- |
| `content_id`    | `uuid`                     | no       | —       |
| `locale`        | `text`                     | no       | —       |
| `title`         | `text`                     | no       | —       |
| `summary`       | `text`                     | no       | —       |
| `body_markdown` | `text`                     | no       | —       |
| `updated_at`    | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                   | Type        | Definition                                                                  |
| -------------------------------------- | ----------- | --------------------------------------------------------------------------- |
| `content_translations_content_id_fkey` | FOREIGN KEY | `FOREIGN KEY (content_id) REFERENCES content_entries(id) ON DELETE CASCADE` |
| `content_translations_locale_check`    | CHECK       | `CHECK (locale = ANY (ARRAY['ja'::text, 'en'::text]))`                      |
| `content_translations_pkey`            | PRIMARY KEY | `PRIMARY KEY (content_id, locale)`                                          |

### Indexes

| Name                        | Definition                                                                                                      |
| --------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `content_translations_pkey` | `CREATE UNIQUE INDEX content_translations_pkey ON public.content_translations USING btree (content_id, locale)` |

## municipality_codes

### Columns

| Column            | Type       | Nullable | Default |
| ----------------- | ---------- | -------- | ------- |
| `code`            | `text`     | no       | —       |
| `prefecture_code` | `smallint` | no       | —       |

### Constraints

| Name                                      | Type        | Definition                                                   |
| ----------------------------------------- | ----------- | ------------------------------------------------------------ |
| `municipality_codes_code_check`           | CHECK       | `CHECK (code ~ '^[0-9]{6}$'::text)`                          |
| `municipality_codes_pkey`                 | PRIMARY KEY | `PRIMARY KEY (code)`                                         |
| `municipality_codes_prefecture_code_fkey` | FOREIGN KEY | `FOREIGN KEY (prefecture_code) REFERENCES prefectures(code)` |

### Indexes

| Name                      | Definition                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------- |
| `municipality_codes_pkey` | `CREATE UNIQUE INDEX municipality_codes_pkey ON public.municipality_codes USING btree (code)` |

## municipality_translations

### Columns

| Column              | Type   | Nullable | Default |
| ------------------- | ------ | -------- | ------- |
| `municipality_code` | `text` | no       | —       |
| `locale`            | `text` | no       | —       |
| `name`              | `text` | no       | —       |
| `name_reading`      | `text` | yes      | —       |

### Constraints

| Name                                               | Type        | Definition                                                                              |
| -------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------- |
| `municipality_translations_locale_check`           | CHECK       | `CHECK (btrim(locale) <> ''::text)`                                                     |
| `municipality_translations_municipality_code_fkey` | FOREIGN KEY | `FOREIGN KEY (municipality_code) REFERENCES municipality_codes(code) ON DELETE CASCADE` |
| `municipality_translations_name_check`             | CHECK       | `CHECK (btrim(name) <> ''::text)`                                                       |
| `municipality_translations_name_reading_check`     | CHECK       | `CHECK (name_reading IS NULL OR btrim(name_reading) <> ''::text)`                       |
| `municipality_translations_pkey`                   | PRIMARY KEY | `PRIMARY KEY (municipality_code, locale)`                                               |

### Indexes

| Name                             | Definition                                                                                                                       |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `municipality_translations_pkey` | `CREATE UNIQUE INDEX municipality_translations_pkey ON public.municipality_translations USING btree (municipality_code, locale)` |

## prefecture_translations

### Columns

| Column            | Type       | Nullable | Default |
| ----------------- | ---------- | -------- | ------- |
| `prefecture_code` | `smallint` | no       | —       |
| `locale`          | `text`     | no       | —       |
| `name`            | `text`     | no       | —       |
| `name_reading`    | `text`     | yes      | —       |

### Constraints

| Name                                           | Type        | Definition                                                                     |
| ---------------------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| `prefecture_translations_locale_check`         | CHECK       | `CHECK (btrim(locale) <> ''::text)`                                            |
| `prefecture_translations_locale_name_key`      | UNIQUE      | `UNIQUE (locale, name)`                                                        |
| `prefecture_translations_name_check`           | CHECK       | `CHECK (btrim(name) <> ''::text)`                                              |
| `prefecture_translations_name_reading_check`   | CHECK       | `CHECK (name_reading IS NULL OR btrim(name_reading) <> ''::text)`              |
| `prefecture_translations_pkey`                 | PRIMARY KEY | `PRIMARY KEY (prefecture_code, locale)`                                        |
| `prefecture_translations_prefecture_code_fkey` | FOREIGN KEY | `FOREIGN KEY (prefecture_code) REFERENCES prefectures(code) ON DELETE CASCADE` |

### Indexes

| Name                                      | Definition                                                                                                                 |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `prefecture_translations_locale_name_key` | `CREATE UNIQUE INDEX prefecture_translations_locale_name_key ON public.prefecture_translations USING btree (locale, name)` |
| `prefecture_translations_pkey`            | `CREATE UNIQUE INDEX prefecture_translations_pkey ON public.prefecture_translations USING btree (prefecture_code, locale)` |

## prefectures

### Columns

| Column | Type       | Nullable | Default |
| ------ | ---------- | -------- | ------- |
| `code` | `smallint` | no       | —       |

### Constraints

| Name                     | Type        | Definition                         |
| ------------------------ | ----------- | ---------------------------------- |
| `prefectures_code_check` | CHECK       | `CHECK (code >= 1 AND code <= 47)` |
| `prefectures_pkey`       | PRIMARY KEY | `PRIMARY KEY (code)`               |

### Indexes

| Name               | Definition                                                                      |
| ------------------ | ------------------------------------------------------------------------------- |
| `prefectures_pkey` | `CREATE UNIQUE INDEX prefectures_pkey ON public.prefectures USING btree (code)` |

## pub_status_translations

### Columns

| Column         | Type       | Nullable | Default |
| -------------- | ---------- | -------- | ------- |
| `status_code`  | `smallint` | no       | —       |
| `locale`       | `text`     | no       | —       |
| `display_name` | `text`     | no       | —       |

### Constraints

| Name                                         | Type        | Definition                                                                  |
| -------------------------------------------- | ----------- | --------------------------------------------------------------------------- |
| `pub_status_translations_display_name_check` | CHECK       | `CHECK (btrim(display_name) <> ''::text)`                                   |
| `pub_status_translations_locale_check`       | CHECK       | `CHECK (btrim(locale) <> ''::text)`                                         |
| `pub_status_translations_pkey`               | PRIMARY KEY | `PRIMARY KEY (status_code, locale)`                                         |
| `pub_status_translations_status_code_fkey`   | FOREIGN KEY | `FOREIGN KEY (status_code) REFERENCES pub_statuses(code) ON DELETE CASCADE` |

### Indexes

| Name                           | Definition                                                                                                             |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| `pub_status_translations_pkey` | `CREATE UNIQUE INDEX pub_status_translations_pkey ON public.pub_status_translations USING btree (status_code, locale)` |

## pub_statuses

### Columns

| Column | Type       | Nullable | Default |
| ------ | ---------- | -------- | ------- |
| `code` | `smallint` | no       | —       |
| `key`  | `text`     | no       | —       |

### Constraints

| Name                      | Type        | Definition           |
| ------------------------- | ----------- | -------------------- |
| `pub_statuses_key_unique` | UNIQUE      | `UNIQUE (key)`       |
| `pub_statuses_pkey`       | PRIMARY KEY | `PRIMARY KEY (code)` |

### Indexes

| Name                      | Definition                                                                             |
| ------------------------- | -------------------------------------------------------------------------------------- |
| `pub_statuses_key_unique` | `CREATE UNIQUE INDEX pub_statuses_key_unique ON public.pub_statuses USING btree (key)` |
| `pub_statuses_pkey`       | `CREATE UNIQUE INDEX pub_statuses_pkey ON public.pub_statuses USING btree (code)`      |

## pub_tags

### Columns

| Column   | Type   | Nullable | Default |
| -------- | ------ | -------- | ------- |
| `pub_id` | `uuid` | no       | —       |
| `tag_id` | `uuid` | no       | —       |

### Constraints

| Name                    | Type        | Definition                                                   |
| ----------------------- | ----------- | ------------------------------------------------------------ |
| `pub_tags_pkey`         | PRIMARY KEY | `PRIMARY KEY (pub_id, tag_id)`                               |
| `pub_tags_pub_id_fkey1` | FOREIGN KEY | `FOREIGN KEY (pub_id) REFERENCES pubs(id) ON DELETE CASCADE` |
| `pub_tags_tag_id_fkey`  | FOREIGN KEY | `FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE` |

### Indexes

| Name                  | Definition                                                                          |
| --------------------- | ----------------------------------------------------------------------------------- |
| `pub_tags_pkey`       | `CREATE UNIQUE INDEX pub_tags_pkey ON public.pub_tags USING btree (pub_id, tag_id)` |
| `pub_tags_tag_id_idx` | `CREATE INDEX pub_tags_tag_id_idx ON public.pub_tags USING btree (tag_id)`          |

## pub_translations

### Columns

| Column         | Type                       | Nullable | Default |
| -------------- | -------------------------- | -------- | ------- |
| `pub_id`       | `uuid`                     | no       | —       |
| `locale`       | `text`                     | no       | —       |
| `name`         | `text`                     | no       | —       |
| `name_reading` | `text`                     | yes      | —       |
| `address`      | `text`                     | yes      | —       |
| `updated_at`   | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                  | Type        | Definition                                                        |
| ------------------------------------- | ----------- | ----------------------------------------------------------------- |
| `pub_translations_address_check`      | CHECK       | `CHECK (address IS NULL OR btrim(address) <> ''::text)`           |
| `pub_translations_locale_check`       | CHECK       | `CHECK (btrim(locale) <> ''::text)`                               |
| `pub_translations_name_check`         | CHECK       | `CHECK (btrim(name) <> ''::text)`                                 |
| `pub_translations_name_reading_check` | CHECK       | `CHECK (name_reading IS NULL OR btrim(name_reading) <> ''::text)` |
| `pub_translations_pkey`               | PRIMARY KEY | `PRIMARY KEY (pub_id, locale)`                                    |
| `pub_translations_pub_id_fkey`        | FOREIGN KEY | `FOREIGN KEY (pub_id) REFERENCES pubs(id) ON DELETE CASCADE`      |

### Indexes

| Name                    | Definition                                                                                          |
| ----------------------- | --------------------------------------------------------------------------------------------------- |
| `pub_translations_pkey` | `CREATE UNIQUE INDEX pub_translations_pkey ON public.pub_translations USING btree (pub_id, locale)` |

## pubs

### Columns

| Column              | Type                       | Nullable | Default             |
| ------------------- | -------------------------- | -------- | ------------------- |
| `id`                | `uuid`                     | no       | `gen_random_uuid()` |
| `latitude`          | `double precision`         | yes      | —                   |
| `longitude`         | `double precision`         | yes      | —                   |
| `website_url`       | `text`                     | yes      | —                   |
| `google_maps_url`   | `text`                     | yes      | —                   |
| `instagram_url`     | `text`                     | yes      | —                   |
| `updated_at`        | `timestamp with time zone` | no       | `now()`             |
| `prefecture_code`   | `smallint`                 | yes      | —                   |
| `status_code`       | `smallint`                 | yes      | —                   |
| `municipality_code` | `text`                     | yes      | —                   |
| `is_published`      | `boolean`                  | no       | `false`             |

### Constraints

| Name                                     | Type        | Definition                                                                                      |
| ---------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| `pubs_columns_new_google_maps_url_check` | CHECK       | `CHECK (google_maps_url IS NULL OR google_maps_url ~* '^https?://'::text)`                      |
| `pubs_columns_new_instagram_url_check`   | CHECK       | `CHECK (instagram_url IS NULL OR instagram_url ~* '^https?://'::text)`                          |
| `pubs_columns_new_latitude_check`        | CHECK       | `CHECK (latitude >= '-90'::integer::double precision AND latitude <= 90::double precision)`     |
| `pubs_columns_new_longitude_check`       | CHECK       | `CHECK (longitude >= '-180'::integer::double precision AND longitude <= 180::double precision)` |
| `pubs_columns_new_pkey`                  | PRIMARY KEY | `PRIMARY KEY (id)`                                                                              |
| `pubs_columns_new_website_url_check`     | CHECK       | `CHECK (website_url IS NULL OR website_url ~* '^https?://'::text)`                              |
| `pubs_municipality_code_fkey`            | FOREIGN KEY | `FOREIGN KEY (municipality_code) REFERENCES municipality_codes(code)`                           |
| `pubs_prefecture_code_fkey`              | FOREIGN KEY | `FOREIGN KEY (prefecture_code) REFERENCES prefectures(code)`                                    |
| `pubs_status_code_fkey`                  | FOREIGN KEY | `FOREIGN KEY (status_code) REFERENCES pub_statuses(code)`                                       |

### Indexes

| Name                         | Definition                                                                               |
| ---------------------------- | ---------------------------------------------------------------------------------------- |
| `pubs_columns_new_pkey`      | `CREATE UNIQUE INDEX pubs_columns_new_pkey ON public.pubs USING btree (id)`              |
| `pubs_municipality_code_idx` | `CREATE INDEX pubs_municipality_code_idx ON public.pubs USING btree (municipality_code)` |
| `pubs_prefecture_code_idx`   | `CREATE INDEX pubs_prefecture_code_idx ON public.pubs USING btree (prefecture_code)`     |
| `pubs_status_code_idx`       | `CREATE INDEX pubs_status_code_idx ON public.pubs USING btree (status_code)`             |

## quiz_choice_translations

### Columns

| Column        | Type                       | Nullable | Default |
| ------------- | -------------------------- | -------- | ------- |
| `question_id` | `text`                     | no       | —       |
| `choice_id`   | `text`                     | no       | —       |
| `locale`      | `text`                     | no       | —       |
| `label`       | `text`                     | no       | —       |
| `updated_at`  | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                    | Type        | Definition                                                                                        |
| --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| `quiz_choice_translations_choice_fkey`  | FOREIGN KEY | `FOREIGN KEY (question_id, choice_id) REFERENCES quiz_choices(question_id, id) ON DELETE CASCADE` |
| `quiz_choice_translations_locale_check` | CHECK       | `CHECK (locale = ANY (ARRAY['ja'::text, 'en'::text]))`                                            |
| `quiz_choice_translations_pkey`         | PRIMARY KEY | `PRIMARY KEY (question_id, choice_id, locale)`                                                    |

### Indexes

| Name                            | Definition                                                                                                                          |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `quiz_choice_translations_pkey` | `CREATE UNIQUE INDEX quiz_choice_translations_pkey ON public.quiz_choice_translations USING btree (question_id, choice_id, locale)` |

## quiz_choices

### Columns

| Column        | Type                       | Nullable | Default |
| ------------- | -------------------------- | -------- | ------- |
| `question_id` | `text`                     | no       | —       |
| `id`          | `text`                     | no       | —       |
| `sort_order`  | `smallint`                 | no       | —       |
| `created_at`  | `timestamp with time zone` | no       | `now()` |
| `updated_at`  | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                   | Type        | Definition                                                                  |
| -------------------------------------- | ----------- | --------------------------------------------------------------------------- |
| `quiz_choices_id_check`                | CHECK       | `CHECK (btrim(id) <> ''::text)`                                             |
| `quiz_choices_pkey`                    | PRIMARY KEY | `PRIMARY KEY (question_id, id)`                                             |
| `quiz_choices_question_id_fkey`        | FOREIGN KEY | `FOREIGN KEY (question_id) REFERENCES quiz_questions(id) ON DELETE CASCADE` |
| `quiz_choices_question_sort_order_key` | UNIQUE      | `UNIQUE (question_id, sort_order)`                                          |
| `quiz_choices_sort_order_check`        | CHECK       | `CHECK (sort_order >= 0)`                                                   |

### Indexes

| Name                                   | Definition                                                                                                              |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `quiz_choices_pkey`                    | `CREATE UNIQUE INDEX quiz_choices_pkey ON public.quiz_choices USING btree (question_id, id)`                            |
| `quiz_choices_question_sort_order_key` | `CREATE UNIQUE INDEX quiz_choices_question_sort_order_key ON public.quiz_choices USING btree (question_id, sort_order)` |

## quiz_question_translations

### Columns

| Column         | Type                       | Nullable | Default |
| -------------- | -------------------------- | -------- | ------- |
| `question_id`  | `text`                     | no       | —       |
| `locale`       | `text`                     | no       | —       |
| `question`     | `text`                     | no       | —       |
| `explanation`  | `text`                     | no       | —       |
| `source_label` | `text`                     | no       | —       |
| `updated_at`   | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                          | Type        | Definition                                                                  |
| --------------------------------------------- | ----------- | --------------------------------------------------------------------------- |
| `quiz_question_translations_locale_check`     | CHECK       | `CHECK (locale = ANY (ARRAY['ja'::text, 'en'::text]))`                      |
| `quiz_question_translations_pkey`             | PRIMARY KEY | `PRIMARY KEY (question_id, locale)`                                         |
| `quiz_question_translations_question_id_fkey` | FOREIGN KEY | `FOREIGN KEY (question_id) REFERENCES quiz_questions(id) ON DELETE CASCADE` |

### Indexes

| Name                              | Definition                                                                                                                   |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `quiz_question_translations_pkey` | `CREATE UNIQUE INDEX quiz_question_translations_pkey ON public.quiz_question_translations USING btree (question_id, locale)` |

## quiz_questions

### Columns

| Column               | Type                       | Nullable | Default |
| -------------------- | -------------------------- | -------- | ------- |
| `id`                 | `text`                     | no       | —       |
| `category`           | `text`                     | yes      | —       |
| `special_month`      | `smallint`                 | yes      | —       |
| `special_day`        | `smallint`                 | yes      | —       |
| `correct_choice_id`  | `text`                     | yes      | —       |
| `source_url`         | `text`                     | yes      | —       |
| `related_content_id` | `uuid`                     | yes      | —       |
| `is_published`       | `boolean`                  | no       | `false` |
| `created_at`         | `timestamp with time zone` | no       | `now()` |
| `updated_at`         | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                                     | Type        | Definition                                                                                                                                                               |
| ---------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `quiz_questions_category_check`          | CHECK       | `CHECK (category IS NULL OR btrim(category) <> ''::text)`                                                                                                                |
| `quiz_questions_correct_choice_fkey`     | FOREIGN KEY | `FOREIGN KEY (id, correct_choice_id) REFERENCES quiz_choices(question_id, id) DEFERRABLE INITIALLY DEFERRED`                                                             |
| `quiz_questions_correct_choice_id_check` | CHECK       | `CHECK (correct_choice_id IS NULL OR btrim(correct_choice_id) <> ''::text)`                                                                                              |
| `quiz_questions_id_check`                | CHECK       | `CHECK (btrim(id) <> ''::text)`                                                                                                                                          |
| `quiz_questions_pkey`                    | PRIMARY KEY | `PRIMARY KEY (id)`                                                                                                                                                       |
| `quiz_questions_related_content_id_fkey` | FOREIGN KEY | `FOREIGN KEY (related_content_id) REFERENCES content_entries(id) ON DELETE SET NULL`                                                                                     |
| `quiz_questions_source_url_check`        | CHECK       | `CHECK (source_url IS NULL OR btrim(source_url) <> ''::text)`                                                                                                            |
| `quiz_questions_special_date_check`      | CHECK       | `CHECK ((special_month IS NULL) = (special_day IS NULL) AND (special_month IS NULL OR special_month >= 1 AND special_month <= 12 AND special_day >= 1 AND special_day <= |
| CASE                                     |

    WHEN special_month = 2 THEN 29
    WHEN special_month = ANY (ARRAY[4, 6, 9, 11]) THEN 30
    ELSE 31

END))` |

### Indexes

| Name                                    | Definition                                                                                                                                                                |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `quiz_questions_admin_list_idx`         | `CREATE INDEX quiz_questions_admin_list_idx ON public.quiz_questions USING btree (updated_at DESC, id)`                                                                   |
| `quiz_questions_category_idx`           | `CREATE INDEX quiz_questions_category_idx ON public.quiz_questions USING btree (category, id)`                                                                            |
| `quiz_questions_pkey`                   | `CREATE UNIQUE INDEX quiz_questions_pkey ON public.quiz_questions USING btree (id)`                                                                                       |
| `quiz_questions_published_idx`          | `CREATE INDEX quiz_questions_published_idx ON public.quiz_questions USING btree (id) WHERE is_published`                                                                  |
| `quiz_questions_related_content_id_idx` | `CREATE INDEX quiz_questions_related_content_id_idx ON public.quiz_questions USING btree (related_content_id) WHERE (related_content_id IS NOT NULL)`                     |
| `quiz_questions_special_date_idx`       | `CREATE INDEX quiz_questions_special_date_idx ON public.quiz_questions USING btree (special_month, special_day, id) WHERE (is_published AND (special_month IS NOT NULL))` |

## schema_migrations

### Columns

| Column       | Type                       | Nullable | Default |
| ------------ | -------------------------- | -------- | ------- |
| `version`    | `text`                     | no       | —       |
| `applied_at` | `timestamp with time zone` | no       | `now()` |

### Constraints

| Name                     | Type        | Definition              |
| ------------------------ | ----------- | ----------------------- |
| `schema_migrations_pkey` | PRIMARY KEY | `PRIMARY KEY (version)` |

### Indexes

| Name                     | Definition                                                                                     |
| ------------------------ | ---------------------------------------------------------------------------------------------- |
| `schema_migrations_pkey` | `CREATE UNIQUE INDEX schema_migrations_pkey ON public.schema_migrations USING btree (version)` |

## tag_translations

### Columns

| Column   | Type   | Nullable | Default |
| -------- | ------ | -------- | ------- |
| `tag_id` | `uuid` | no       | —       |
| `locale` | `text` | no       | —       |
| `name`   | `text` | no       | —       |

### Constraints

| Name                               | Type        | Definition                                                   |
| ---------------------------------- | ----------- | ------------------------------------------------------------ |
| `tag_translations_locale_check`    | CHECK       | `CHECK (btrim(locale) <> ''::text)`                          |
| `tag_translations_locale_name_key` | UNIQUE      | `UNIQUE (locale, name)`                                      |
| `tag_translations_name_check`      | CHECK       | `CHECK (btrim(name) <> ''::text)`                            |
| `tag_translations_pkey`            | PRIMARY KEY | `PRIMARY KEY (tag_id, locale)`                               |
| `tag_translations_tag_id_fkey`     | FOREIGN KEY | `FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE` |

### Indexes

| Name                               | Definition                                                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `tag_translations_locale_name_key` | `CREATE UNIQUE INDEX tag_translations_locale_name_key ON public.tag_translations USING btree (locale, name)` |
| `tag_translations_pkey`            | `CREATE UNIQUE INDEX tag_translations_pkey ON public.tag_translations USING btree (tag_id, locale)`          |

## tags

### Columns

| Column | Type   | Nullable | Default             |
| ------ | ------ | -------- | ------------------- |
| `id`   | `uuid` | no       | `gen_random_uuid()` |
| `key`  | `text` | no       | —                   |

### Constraints

| Name              | Type        | Definition         |
| ----------------- | ----------- | ------------------ |
| `tags_key_unique` | UNIQUE      | `UNIQUE (key)`     |
| `tags_pkey`       | PRIMARY KEY | `PRIMARY KEY (id)` |

### Indexes

| Name              | Definition                                                             |
| ----------------- | ---------------------------------------------------------------------- |
| `tags_key_unique` | `CREATE UNIQUE INDEX tags_key_unique ON public.tags USING btree (key)` |
| `tags_pkey`       | `CREATE UNIQUE INDEX tags_pkey ON public.tags USING btree (id)`        |
