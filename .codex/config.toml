# Project-scoped Codex configuration.
# Codex loads this file as a trusted project override from `.codex/config.toml`.

sandbox_mode = "workspace-write"
approval_policy = "on-request"
web_search = "cached"
project_doc_max_bytes = 32768
